#include "study_algorithm.h"

void Study_algorithm::about_algorithm()
{
    std::cout << "The algorithms library defines functions for a variety of purposes (e.g. searching, sorting, counting, manipulating)"
                 " that operate on ranges of elements. Note that a range is defined as "
                 "[first, last) where last refers to the element past the last element to inspect or modify" << std::endl;
}
